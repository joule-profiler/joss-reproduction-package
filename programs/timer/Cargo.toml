[package]
name = "timer"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
timerfd = "1.6.0"
tokio = { version = "1.25", features = ["time", "rt-multi-thread", "macros", "sync", "rt"] }